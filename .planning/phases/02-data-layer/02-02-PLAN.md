---
phase: 02-data-layer
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - stores/session-store.ts
autonomous: true
requirements: [DATA-03, DATA-04]

must_haves:
  truths:
    - "Zustand store holds session state (preferences, pool, card index, filters)"
    - "Store state persists to localStorage across page refresh"
  artifacts:
    - path: stores/session-store.ts
      provides: Single Zustand store with persist middleware
      contains: "persist"
  key_links:
    - from: stores/session-store.ts
      to: lib/types/database.types.ts
      via: import SessionState, Recipe
      pattern: "SessionState|Recipe"
    - from: stores/session-store.ts
      to: localStorage
      via: persist middleware + custom safe storage adapter
      pattern: "createJSONStorage|partialize"
---

<objective>
Single Zustand store with session state shape, persist middleware, partialize to exclude actions, and custom storage adapter with try/catch for Safari/private browsing safety.

Purpose: Session state (preferences, pool, card index, filters) persists across refresh. Per CONTEXT: persist full Recipe objects in pool for V0.
Output: stores/session-store.ts
</objective>

<execution_context>
@/Users/shubham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shubham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer/02-CONTEXT.md
@.planning/phases/02-data-layer/02-RESEARCH.md
@.planning/phases/02-data-layer/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create session store with persist and safe storage</name>
  <files>stores/session-store.ts</files>
  <action>
Create stores/session-store.ts per CONTEXT and RESEARCH:

State shape (SessionState from 02-01):
- sessionId: string
- preferences: { diet: string | null, blocklist: string[], onboardingComplete: boolean }
- session: { cuisines: string[], ingredientFilter: string | null, pool: Recipe[], currentIndex: number, lastActiveAt: string, setupComplete: boolean }

Implementation:
- Use create from zustand with persist middleware
- Custom StateStorage adapter wrapping localStorage: getItem/setItem/removeItem each in try/catch; on error return null (getItem) or no-op (setItem/removeItem) — required for Safari private browsing (QuotaExceededError)
- partialize: return only { sessionId, preferences, session } — exclude ALL actions/functions
- name: 'akk-session'
- storage: createJSONStorage(() =&gt; safeStorage)

Actions: setSessionId, setPreferences, setSession, setPool, setCurrentIndex, incrementIndex, setLastActiveAt, resetSession, etc. (Claude's discretion on exact names). Include _hasHydrated: boolean for hydration gate.

Initial state: sessionId '', preferences default, session default (pool: [], currentIndex: 0, setupComplete: false).
  </action>
  <verify>stores/session-store.ts exists; persist with partialize; safe storage with try/catch; npm run build passes</verify>
  <done>Zustand store created with persist middleware</done>
</task>

<task type="auto">
  <name>Task 2: Wire hydration for Next.js</name>
  <files>stores/session-store.ts</files>
  <action>
Add hydration handling to avoid server/client mismatch:
- Use skipHydration: true in persist config
- In store, add onRehydrateStorage callback or use persist.getStorage() to call rehydrate in a client-only effect
- Export a useHydrateStore hook or document that consumers must call persist.rehydrate() in useEffect before reading persisted state, OR gate persisted-state UI behind _hasHydrated flag

Per RESEARCH: Zustand persist is async; server has no localStorage. Use skipHydration + manual rehydrate() in useEffect, or _hasHydrated gate.
  </action>
  <verify>Hydration handled; no "Text content does not match" errors when loading app with persisted state</verify>
  <done>Store rehydrates correctly; persisted state available after mount</done>
</task>

</tasks>

<verification>
- npm run build passes
- In browser: set store state (e.g. preferences.diet = 'vegetarian'), refresh page — state persists
- Safari private browsing: app does not crash on write (try/catch prevents throw)
</verification>

<success_criteria>
- DATA-03: Zustand store holds session state (preferences, pool, card index, filters)
- DATA-04: Persist middleware syncs to localStorage; state survives refresh
</success_criteria>

<output>
After completion, create .planning/phases/02-data-layer/02-02-SUMMARY.md
</output>
