---
phase: 02-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - lib/supabase/client.ts
  - lib/types/database.types.ts
  - services/interaction-logger.ts
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "Recipe data can be fetched from Supabase with anon key"
    - "TypeScript interfaces exist for Recipe, UserSession, UserInteraction, SessionState"
    - "Fire-and-forget interaction logger ready for consumers"
  artifacts:
    - path: lib/supabase/client.ts
      provides: Supabase browser client singleton
      contains: createClient
    - path: lib/types/database.types.ts
      provides: Database, Recipe, UserSession, UserInteraction, SessionState types
      contains: "Tables<'recipes'>"
    - path: services/interaction-logger.ts
      provides: logInteraction fire-and-forget function
      exports: ["logInteraction"]
  key_links:
    - from: lib/supabase/client.ts
      to: process.env.NEXT_PUBLIC_SUPABASE_URL
      via: env vars
      pattern: "NEXT_PUBLIC_SUPABASE"
    - from: services/interaction-logger.ts
      to: lib/supabase/client.ts
      via: import supabase
      pattern: "from.*supabase/client"
---

<objective>
Supabase client, TypeScript types (Recipe, UserSession, UserInteraction, SessionState), and fire-and-forget interaction logger ready for all data consumers.

Purpose: Foundation for recipe fetches, session state, and signal logging. No SSR or auth for V0.
Output: lib/supabase/client.ts, lib/types/database.types.ts, services/interaction-logger.ts
</objective>

<execution_context>
@/Users/shubham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shubham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer/02-CONTEXT.md
@.planning/phases/02-data-layer/02-RESEARCH.md
@scripts/schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps, generate types, add SessionState</name>
  <files>package.json, lib/types/database.types.ts</files>
  <action>
Install @supabase/supabase-js and zustand:
  npm install @supabase/supabase-js zustand

Generate Supabase types (creates lib/types/ if needed):
  npx supabase gen types typescript --project-id hrmhnovohubkfyxipjog &gt; lib/types/database.types.ts

Add to lib/types/database.types.ts:
- Export type aliases: Recipe = Tables&lt;'recipes'&gt;, UserSession = Tables&lt;'user_sessions'&gt;, UserInteraction = Tables&lt;'user_interactions'&gt;
- Add SessionState interface per CONTEXT store shape: sessionId (string), preferences (diet, blocklist, onboardingComplete), session (cuisines, ingredientFilter, pool: Recipe[], currentIndex, lastActiveAt, setupComplete)
  </action>
  <verify>lib/types/database.types.ts exists; Recipe, UserSession, UserInteraction, SessionState exported; npm run build passes</verify>
  <done>All TypeScript interfaces exist and are usable</done>
</task>

<task type="auto">
  <name>Task 2: Create Supabase client</name>
  <files>lib/supabase/client.ts</files>
  <action>
Create lib/supabase/client.ts:
- Import createClient from @supabase/supabase-js
- Import type Database from @/lib/types/database.types
- Create singleton: createClient&lt;Database&gt;(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!)
- Export as `supabase`

Per CONTEXT: .env.local already has NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY.
  </action>
  <verify>lib/supabase/client.ts exists, exports supabase; npm run build passes</verify>
  <done>Supabase client configured for public recipe reads</done>
</task>

<task type="auto">
  <name>Task 3: Create interaction logger</name>
  <files>services/interaction-logger.ts</files>
  <action>
Create services/interaction-logger.ts per CONTEXT and RESEARCH:
- Import supabase from @/lib/supabase/client
- Export function logInteraction(sessionId: string, action: AllowedAction, recipeId?: string, metadata?: Record&lt;string, unknown&gt;)
- Allowed actions: swipe_next, swipe_prev, tap, youtube_open, web_open, found_my_pick, back_no_action, shuffle, session_success_inferred, filter_change (match schema.sql RLS)
- Guard: if sessionId.length &lt; 8 return early
- Fire-and-forget: supabase.from('user_interactions').insert({ session_id, recipe_id: recipeId ?? null, action, metadata: metadata ?? null }) — NO await
- Do NOT block or surface errors to caller
  </action>
  <verify>services/interaction-logger.ts exists; logInteraction is exported; no await on insert</verify>
  <done>Interaction logger ready for discovery loop and recipe detail consumers</done>
</task>

</tasks>

<verification>
- npm run build passes
- supabase.from('recipes').select('*').limit(1) returns data when run in browser console (manual smoke test)
- logInteraction can be called without errors
</verification>

<success_criteria>
- DATA-01: Supabase client configured with anon key
- DATA-02: Recipe, UserSession, UserInteraction, SessionState interfaces exist
- Interaction logger (Phase 2 scope per CONTEXT) ready
</success_criteria>

<output>
After completion, create .planning/phases/02-data-layer/02-01-SUMMARY.md
</output>
