---
phase: 07-recipe-detail-signal-logging
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - app/page.tsx
  - components/discovery/DiscoveryCardStack.tsx
  - components/discovery/DiscoveryCard.tsx
  - components/discovery/FilterBar.tsx
autonomous: true
requirements:
  - DETL-06
  - LOGG-01
  - LOGG-02
  - LOGG-03

must_haves:
  truths:
    - "Tap on card opens RecipeDetailOverlay; back returns to discovery at same card position"
    - "Swipe next/prev, tap, shuffle, link opens, pick, back — all logged fire-and-forget"
    - "Picked recipes show badge on discovery card; viewed recipes show subtle indicator"
  artifacts:
    - path: app/page.tsx
      provides: "RecipeDetailOverlay integration, recordViewed on tap"
      contains: "RecipeDetailOverlay"
    - path: components/discovery/DiscoveryCardStack.tsx
      provides: "logInteraction for swipe_next, swipe_prev, tap"
      contains: "logInteraction"
    - path: components/discovery/FilterBar.tsx
      provides: "logInteraction for shuffle"
      contains: "logInteraction"
    - path: components/discovery/DiscoveryCard.tsx
      provides: "Picked and viewed badges"
      contains: "pickedIds"
  key_links:
    - from: app/page.tsx
      to: components/discovery/RecipeDetailOverlay.tsx
      via: "Replace placeholder overlay"
      pattern: "RecipeDetailOverlay"
    - from: components/discovery/DiscoveryCardStack.tsx
      to: services/interaction-logger.ts
      via: "logInteraction at handleDragEnd and onCardTap"
      pattern: "logInteraction"
---

<objective>
Wire RecipeDetailOverlay into page, replace placeholder. Add fire-and-forget logInteraction at all discovery-level call sites (swipe, tap, shuffle). Add picked and viewed badges to DiscoveryCard. Ensure back returns to same card position (no route change, currentIndex preserved).

Purpose: Complete Phase 7 integration. All interactions logged; overlay replaces placeholder; card badges reflect picked/viewed state.
Output: page.tsx, DiscoveryCardStack.tsx, DiscoveryCard.tsx, FilterBar.tsx
</objective>

<execution_context>
@/Users/shubham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shubham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-recipe-detail-signal-logging/07-RESEARCH.md
@.planning/phases/07-recipe-detail-signal-logging/07-CONTEXT.md
@.planning/phases/07-recipe-detail-signal-logging/07-01-PLAN.md
@.planning/phases/07-recipe-detail-signal-logging/07-02-PLAN.md
@app/page.tsx
@components/discovery/DiscoveryCardStack.tsx
@components/discovery/DiscoveryCard.tsx
@components/discovery/FilterBar.tsx
@services/interaction-logger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Page integration — RecipeDetailOverlay, recordViewed</name>
  <files>app/page.tsx</files>
  <action>
Replace the placeholder overlay (lines 86-123) with RecipeDetailOverlay. Pass recipe={selectedRecipe}, open={!!selectedRecipe}, onClose={() => setSelectedRecipe(null)}.

When onCardTap fires (user taps card): before setSelectedRecipe(recipe), call recordViewed(recipe.id) from store. This marks the recipe as viewed for the badge.

Ensure selectedRecipe state does not affect currentIndex — closing overlay returns to same card. No route change; overlay is controlled by selectedRecipe only.
  </action>
  <verify>grep -E "RecipeDetailOverlay|recordViewed" app/page.tsx</verify>
  <done>RecipeDetailOverlay replaces placeholder; recordViewed called on tap; back returns to same card</done>
</task>

<task type="auto">
  <name>Task 2: Wire logInteraction — swipe, tap, shuffle</name>
  <files>components/discovery/DiscoveryCardStack.tsx, components/discovery/FilterBar.tsx</files>
  <action>
**DiscoveryCardStack:** Import logInteraction from services/interaction-logger. Get sessionId from useSessionStore(s => s.sessionId). In handleDragEnd, when nextCard or prevCard is called, also call logInteraction(sessionId, 'swipe_next'|'swipe_prev', currentRecipe?.id). In onCardTap callback (passed to DiscoveryCard), before invoking the parent onCardTap, call logInteraction(sessionId, 'tap', recipe.id). Fire-and-forget: no await.

**FilterBar:** Import logInteraction. Get sessionId from store. In shuffle button onClick, before shufflePool(), call logInteraction(sessionId, 'shuffle'). Fire-and-forget.

Overlay already logs youtube_open, web_open, found_my_pick, back_no_action per 07-02.
  </action>
  <verify>grep -E "logInteraction.*swipe_next|logInteraction.*tap|logInteraction.*shuffle" components/discovery/*.tsx</verify>
  <done>swipe_next, swipe_prev, tap, shuffle logged at call sites</done>
</task>

<task type="auto">
  <name>Task 3: DiscoveryCard — picked and viewed badges</name>
  <files>components/discovery/DiscoveryCard.tsx</files>
  <action>
DiscoveryCard receives pickedIds and viewedIds (or isPicked, isViewed booleans) as props. Page/DiscoveryCardStack passes these from useSessionStore(s => ({ pickedIds: s.pickedIds, viewedIds: s.viewedIds })).

**Picked badge:** When recipe.id in pickedIds, show small checkmark badge (e.g. top-right corner, material-symbols check_circle or similar). Per CONTEXT: "Picked recipes show a small badge/checkmark on the discovery card."

**Viewed indicator:** When recipe.id in viewedIds, apply subtle visual change — e.g. slight opacity reduction (opacity-90) or a small "seen" dot. Per CONTEXT: "Viewed recipes show a subtle visual change on the card (slight dim or seen indicator)."

DiscoveryCardStack must pass pickedIds and viewedIds to DiscoveryCard. Get from store.
  </action>
  <verify>grep -E "pickedIds|viewedIds|isPicked|isViewed" components/discovery/DiscoveryCard.tsx components/discovery/DiscoveryCardStack.tsx</verify>
  <done>Picked recipes show checkmark badge; viewed recipes show subtle indicator</done>
</task>

</tasks>

<verification>
- RecipeDetailOverlay integrated; back returns to same card
- logInteraction called for swipe_next, swipe_prev, tap, shuffle (overlay logs in 07-02)
- DiscoveryCard shows picked badge and viewed indicator
- All logging fire-and-forget (no await)
</verification>

<success_criteria>
- Full Phase 7 flow: tap → overlay → links → pick → back → same card
- All 8 actions logged: swipe_next, swipe_prev, tap, youtube_open, web_open, found_my_pick, back_no_action, shuffle
- Card badges reflect state
</success_criteria>

<output>
After completion, create `.planning/phases/07-recipe-detail-signal-logging/07-03-SUMMARY.md`
</output>
