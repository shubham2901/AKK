---
phase: 07-recipe-detail-signal-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stores/session-store.ts
  - lib/utils/recipe-urls.ts
autonomous: true
requirements:
  - LOGG-04
  - DETL-02
  - DETL-04
  - DETL-05

must_haves:
  truths:
    - "Session ID is set before any logging (enables logInteraction)"
    - "Store has pickedIds and viewedIds for card badges and toggle state"
    - "Web recipe URL can be extracted from description when web_recipe_link is null"
    - "Source attribution for YouTube and web links is derivable from recipe data"
  artifacts:
    - path: stores/session-store.ts
      provides: "sessionId in startSession, pickedIds, viewedIds, togglePick, recordViewed"
      contains: "togglePick"
    - path: lib/utils/recipe-urls.ts
      provides: "extractWebRecipeUrl, getYouTubeAttribution, getWebAttribution"
      exports: ["extractWebRecipeUrl", "getYouTubeAttribution", "getWebAttribution"]
  key_links:
    - from: stores/session-store.ts
      to: "startSession"
      via: "setSessionId when sessionId empty"
      pattern: "crypto\\.randomUUID"
---

<objective>
Session foundation for Phase 7: session ID generation so logging works, and store extensions for picked/viewed state. Recipe URL utility for extracting web links and source attribution from descriptions.

Purpose: logInteraction requires session_id; Phase 8 creates full session management later. Recipe URL extraction enables "Full Recipe" link when web_recipe_link is null; attribution enables "YouTube / Kunal Kapur" style labels.
Output: session-store.ts with sessionId in startSession, pickedIds/viewedIds/togglePick/recordViewed; lib/utils/recipe-urls.ts
</objective>

<execution_context>
@/Users/shubham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shubham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-recipe-detail-signal-logging/07-RESEARCH.md
@.planning/phases/07-recipe-detail-signal-logging/07-CONTEXT.md
@stores/session-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session store — sessionId, pickedIds, viewedIds</name>
  <files>stores/session-store.ts</files>
  <action>
In startSession: if sessionId is empty (length &lt; 8), generate crypto.randomUUID() and call setSessionId(id). This ensures session_id exists for logInteraction before Phase 8.

Add to session state (or store root): pickedIds: string[], viewedIds: string[].
Add actions: togglePick(recipeId: string) — add/remove from pickedIds; recordViewed(recipeId: string) — add to viewedIds if not already present.
Update partialize to persist pickedIds (sessionId already persisted). viewedIds can be session-only (no persist) per CONTEXT "viewed recipes show subtle visual change on card".
Add pickedIds and viewedIds to Session type in database.types if needed, or extend store root. Session type is in database.types — check if Session extends to include these or if they live at store root. Research says "extend session store with pickedIds, viewedIds". Use store root: { sessionId, preferences, session, pickedIds, viewedIds } so Session type stays unchanged.
  </action>
  <verify>grep -E "togglePick|recordViewed|pickedIds|viewedIds|setSessionId" stores/session-store.ts | head -20</verify>
  <done>startSession sets sessionId when empty; togglePick and recordViewed exist; pickedIds and viewedIds in state</done>
</task>

<task type="auto">
  <name>Task 2: Recipe URL utility</name>
  <files>lib/utils/recipe-urls.ts</files>
  <action>
Create lib/utils/recipe-urls.ts with:

1. extractWebRecipeUrl(description: string | null): string | null — regex for known domains (hebbarskitchen.com, archanaskitchen.com, sanjeevkapoor.com) per 07-RESEARCH. Return first match or null.

2. getYouTubeAttribution(recipe: Recipe): string — use recipe.title as primary; fallback "YouTube" if null. Format: "YouTube / {title}" or "YouTube".

3. getWebAttribution(recipe: Recipe): string — use recipe.web_recipe_link ?? extractWebRecipeUrl(recipe.description). Parse hostname, map known domains: hebbarskitchen.com → "Hebbar's Kitchen", archanaskitchen.com → "Archana's Kitchen". Return mapped name or hostname without www. Return empty string if no URL.

Import Recipe from @/lib/types/database.types.
  </action>
  <verify>grep -E "export function (extractWebRecipeUrl|getYouTubeAttribution|getWebAttribution)" lib/utils/recipe-urls.ts</verify>
  <done>extractWebRecipeUrl, getYouTubeAttribution, getWebAttribution exported and usable</done>
</task>

</tasks>

<verification>
- startSession sets sessionId when empty; togglePick/recordViewed work
- recipe-urls.ts exports extractWebRecipeUrl, getYouTubeAttribution, getWebAttribution
- No new packages needed
</verification>

<success_criteria>
- Session ID set before discovery; logInteraction will not early-return
- Picked and viewed state available for overlay and badges
- Web recipe URL and attribution derivable from recipe data
</success_criteria>

<output>
After completion, create `.planning/phases/07-recipe-detail-signal-logging/07-01-SUMMARY.md`
</output>
