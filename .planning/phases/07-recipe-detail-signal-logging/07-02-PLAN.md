---
phase: 07-recipe-detail-signal-logging
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - components/discovery/RecipeDetailOverlay.tsx
autonomous: true
requirements:
  - DETL-01
  - DETL-02
  - DETL-03
  - DETL-04
  - DETL-05
  - DETL-07

must_haves:
  truths:
    - "Recipe detail slides up as overlay (almost-full-screen, peek of card at top)"
    - "Shows food photo, recipe name, cuisine/diet chips, one-line hook"
    - "YouTube and web link cards open in new tab with source attribution"
    - "Found my pick CTA toggles to Picked, shows toast, haptic, logs found_my_pick"
    - "Back arrow and swipe-down dismiss overlay"
  artifacts:
    - path: components/discovery/RecipeDetailOverlay.tsx
      provides: "Full recipe detail overlay per editorial mockup"
      min_lines: 150
  key_links:
    - from: components/discovery/RecipeDetailOverlay.tsx
      to: lib/utils/recipe-urls.ts
      via: "getYouTubeAttribution, getWebAttribution, extractWebRecipeUrl"
      pattern: "recipe-urls"
    - from: components/discovery/RecipeDetailOverlay.tsx
      to: services/interaction-logger.ts
      via: "logInteraction for youtube_open, web_open, found_my_pick, back_no_action"
      pattern: "logInteraction"
---

<objective>
RecipeDetailOverlay component: almost-full-screen slide-up sheet over discovery card. Layout matches editorial mockup (stitch_screens/5_recipe_detail_editorial.html). Back arrow + swipe-down dismiss. YouTube and web link cards with source attribution. "Found my pick" toggle with toast and haptic. Fire-and-forget logging for overlay actions.

Purpose: User taps card to see full recipe, links, and pick CTA. Overlay is overlay (not full page nav). Opaque background, no dimmed backdrop per CONTEXT.
Output: components/discovery/RecipeDetailOverlay.tsx
</objective>

<execution_context>
@/Users/shubham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shubham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-recipe-detail-signal-logging/07-RESEARCH.md
@.planning/phases/07-recipe-detail-signal-logging/07-CONTEXT.md
@stitch_screens/5_recipe_detail_editorial.html
@components/discovery/FilterBottomSheet.tsx
@lib/utils/recipe-urls.ts
@services/interaction-logger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RecipeDetailOverlay — slide-up sheet, layout, links, CTA</name>
  <files>components/discovery/RecipeDetailOverlay.tsx</files>
  <action>
Create RecipeDetailOverlay.tsx. Props: recipe: Recipe, open: boolean, onClose: () => void.

**Overlay behavior (per CONTEXT):**
- Almost-full-screen slide-up — height calc(100dvh - 48px) or top: 48px so discovery card peeks at top
- Opaque background (bg-bg-light), NO dimmed backdrop — do not add bg-charcoal/30 behind overlay
- Dismissible via back arrow (top-left) AND swipe-down gesture
- Use motion.div with animate={{ y: open ? 0 : '100%' }}, transition spring (stiffness 300, damping 30)
- Put drag="y" on a narrow drag handle strip at top (per 07-RESEARCH Pitfall 2 — avoids scroll conflict). Content below is scrollable (overflow-y-auto). onDragEnd: if offset.y &gt; 100 or velocity.y &gt; 500, call onClose
- On close: logInteraction(sessionId, 'back_no_action', recipe.id) fire-and-forget, then onClose()

**Layout (per mockup):**
- Back arrow nav (top-left): arrow_back icon + "Back" text, onClick → log back_no_action, onClose
- Hero image: aspect-square or aspect-video, hard border (border-2 border-charcoal), rounded-xl, bg-charcoal overflow-hidden. Use recipe.thumbnail
- Title: font-heading font-black text-6xl uppercase huge-title (line-height 0.9, letter-spacing -0.04em)
- Meta chips: cook_time (e.g. "30 Mins"), difficulty, cuisine — border-primary, text-primary, hide if null
- Hook: recipe.one_line_hook, text-xl font-medium text-charcoal/80
- Divider: h-[2px] bg-charcoal
- Link section: "Follow the guides" heading. YouTube card: play_circle icon, "Video Tutorial", getYouTubeAttribution(recipe). Web card (only if URL exists): menu_book icon, "Step-by-Step Article", getWebAttribution(recipe). Use recipe.url for YouTube, recipe.web_recipe_link ?? extractWebRecipeUrl(recipe.description) for web. Both: target="_blank" rel="noopener noreferrer". On click: logInteraction(sessionId, 'youtube_open'|'web_open', recipe.id)
- Sticky CTA: fixed bottom, gradient fade (from-bg-light via-bg-light to-transparent), full-width button "Found my pick" (Burnt Orange fill, border-2 border-charcoal). When picked: "Picked ✓", different color (e.g. bg-charcoal/20 or success state). Toggle: call togglePick(recipe.id), triggerHaptic (feature-detect navigator.vibrate), show toast "Added to shortlist" or "Picked!" (fixed div, motion fade, 2s), logInteraction(sessionId, 'found_my_pick', recipe.id, { picked: true/false })

**Haptic:** if ('vibrate' in navigator) { navigator.vibrate(10) } catch no-op. Per research: Safari doesn't support, fail silently.

**Toast:** Simple fixed div (e.g. bottom-24), motion AnimatePresence, 2s then fade out. No new library.
  </action>
  <verify>grep -E "RecipeDetailOverlay|logInteraction|togglePick|getYouTubeAttribution|extractWebRecipeUrl" components/discovery/RecipeDetailOverlay.tsx | head -15</verify>
  <done>Overlay slides up, shows full layout, links open in new tab, CTA toggles, back and swipe-down dismiss</done>
</task>

</tasks>

<verification>
- RecipeDetailOverlay renders per mockup layout
- No dimmed backdrop (opaque only)
- Links use target="_blank" rel="noopener noreferrer"
- logInteraction called for youtube_open, web_open, found_my_pick, back_no_action
- Drag handle only (not full sheet) to avoid scroll conflict
</verification>

<success_criteria>
- Recipe detail overlay matches editorial design
- All overlay interactions logged fire-and-forget
- Found my pick toggle works with toast and haptic
</success_criteria>

<output>
After completion, create `.planning/phases/07-recipe-detail-signal-logging/07-02-SUMMARY.md`
</output>
