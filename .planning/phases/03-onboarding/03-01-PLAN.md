---
phase: 03-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - components/onboarding/OnboardingFlow.tsx
  - components/onboarding/DietStep.tsx
autonomous: true
requirements: [ONBR-01, ONBR-04]

must_haves:
  truths:
    - "User sees diet selection (3 vertical cards with icons) on first launch"
    - "Single-select: tapping a card selects it and deselects the previous"
    - "Let's go button advances to next step (blocklist, built in 03-02)"
    - "Onboarding renders only when preferences.onboardingComplete is false"
  artifacts:
    - path: components/onboarding/OnboardingFlow.tsx
      provides: Step state machine for onboarding flow
      exports: ["default"]
    - path: components/onboarding/DietStep.tsx
      provides: Diet preference selection screen
      exports: ["default"]
  key_links:
    - from: app/page.tsx
      to: components/onboarding/OnboardingFlow.tsx
      via: conditional render on onboardingComplete
      pattern: "preferences.onboardingComplete"
    - from: components/onboarding/DietStep.tsx
      to: stores/session-store.ts
      via: useSessionStore setDiet
      pattern: "setDiet"
---

<objective>
Install motion library, add Material Symbols font, build the diet preference screen with 3 vertical cards (Vegetarian / Non-Veg / Vegan), and set up the onboarding gate in page.tsx.

Purpose: First screen of onboarding — user picks their diet. Advancing calls setDiet() and moves to step 2 (blocklist, built in 03-02).
Output: components/onboarding/OnboardingFlow.tsx, components/onboarding/DietStep.tsx, updated page.tsx
</objective>

<execution_context>
@/Users/shubham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shubham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DESIGN_SYSTEM.md
@.planning/phases/03-onboarding/03-CONTEXT.md
@.planning/phases/03-onboarding/03-RESEARCH.md
@stores/session-store.ts
@app/layout.tsx
@app/page.tsx
@app/globals.css
@stitch_screens/2_diet_preference.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install motion, add Material Symbols font</name>
  <files>package.json, app/layout.tsx</files>
  <action>
Install motion library:
  npm install motion

Add Material Symbols Outlined font to layout.tsx:
- Import next/head is NOT used in App Router. Instead, add a `<link>` tag in the `<head>` section.
- In Next.js App Router, use the metadata API or add the link directly in the `<head>` of the html element in layout.tsx.
- Add: `<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet" />`
- Place it inside the `<head>` element (between `<html>` and `<body>` in layout.tsx, or use Next.js metadata `icons` / `other` if appropriate)

NOTE: In Next.js App Router, you can add arbitrary `<link>` tags inside the `<html>` using the `<head>` element or by placing them inside `<body>` — either works for external font stylesheets. The simplest approach is placing the link tag at the top of the body or using metadata.
  </action>
  <verify>npm ls motion shows it installed; layout.tsx has the Material Symbols link; npm run build passes</verify>
  <done>Motion library installed and Material Symbols font available</done>
</task>

<task type="auto">
  <name>Task 2: Create DietStep component</name>
  <files>components/onboarding/DietStep.tsx</files>
  <action>
Create components/onboarding/DietStep.tsx as a 'use client' component.

Props: `onNext: () => void`

Internal state: `selected: DietPreference | null` (local state, not yet committed to store)

Layout (reference stitch_screens/2_diet_preference.html, but match vibe not pixels):
- Full height container: `min-h-screen flex flex-col max-w-md mx-auto`
- Header row: spacer (no back arrow on step 1) + "Step 1 of 2" pill badge + spacer
  - Pill: `px-4 py-1 border-2 border-charcoal rounded-full bg-white text-sm font-extrabold uppercase tracking-wider`
- Headline section: large heading using font-heading class
  - Text: "WHAT DO YOU EAT?" with "EAT?" in primary color
  - Style: `text-6xl sm:text-7xl font-extrabold uppercase tracking-[-0.04em] leading-[0.85]`
- Three vertical cards in a horizontal flex row: `flex flex-1 gap-3 px-6 pb-8`
  - Each card: `flex-1 flex flex-col items-center justify-between p-4 border-2 border-charcoal rounded-xl`
  - Unselected: `bg-white`
  - Selected: `bg-primary shadow-medium` (shadow-medium = 6px 6px 0px #181311)
  - Each card contains:
    - Circle icon container: `w-full aspect-square rounded-full border-2 border-charcoal flex items-center justify-center`
      - Vegetarian: bg-[#A8E6CF], icon "eco"
      - Non-Veg: bg-[#FFD3B6], icon "kebab_dining"
      - Vegan: bg-[#DCEDC1], icon "potted_plant"
    - Label: `text-sm font-extrabold uppercase tracking-tighter text-center`
      - Unselected: `text-charcoal`
      - Selected: `text-white`
  - On tap: set local selected state; use motion.div with whileTap={{ scale: 0.95 }} for feedback
- Bottom CTA: sticky bottom area with border-top
  - "Let's go" button: full-width, bg-primary, border-2, shadow-medium, active:translate-y-1 active:shadow-none
  - Text: "Let's go" in heading font, white, bold, uppercase, italic
  - Arrow icon: material-symbols-outlined "arrow_forward"
  - Disabled state (no diet selected): opacity-50, pointer-events-none
  - On click: call `useSessionStore.getState().setDiet(selected)` then call `onNext()`

Icon rendering: Use `<span className="material-symbols-outlined text-4xl text-charcoal" style={{ fontVariationSettings: "'FILL' 1" }}>icon_name</span>`

IMPORTANT: The component must be `'use client'` since it uses useState, event handlers, and Zustand hooks.
  </action>
  <verify>Component renders 3 cards; tapping one selects it visually; "Let's go" is disabled until selection made; no TypeScript errors</verify>
  <done>Diet preference screen renders with 3 selectable cards and CTA</done>
</task>

<task type="auto">
  <name>Task 3: Create OnboardingFlow + wire page.tsx gate</name>
  <files>components/onboarding/OnboardingFlow.tsx, app/page.tsx</files>
  <action>
Create components/onboarding/OnboardingFlow.tsx as a 'use client' component:
- Internal state: `step` (number, starts at 0)
- step 0: render DietStep with onNext={() => setStep(1)}
- step 1: placeholder div with text "Blocklist step coming in 03-02" (will be replaced in next plan)
- Wrap children in AnimatePresence from motion/react with mode="wait"
- Each step wrapped in motion.div with key, initial/animate/exit for slide transition:
  - initial: { x: 300, opacity: 0 }
  - animate: { x: 0, opacity: 1 }
  - exit: { x: -300, opacity: 0 }
  - transition: { type: 'spring', stiffness: 300, damping: 30 }

Update app/page.tsx:
- Add 'use client' directive
- Import useSessionStore from @/stores/session-store
- Import OnboardingFlow from @/components/onboarding/OnboardingFlow
- Hydration gate: if (!hasHydrated) return null (prevents flash)
- Onboarding gate: if (!onboardingComplete) return <OnboardingFlow />
- Keep existing placeholder content for the "logged in" state (post-onboarding)

CRITICAL: The hydration gate (return null when !_hasHydrated) prevents users from seeing onboarding flash on return visits. The body background is already #FFF9F5, so returning null shows a warm white screen — no white flash.
  </action>
  <verify>On first visit (clear localStorage), diet step renders; selecting a diet and clicking "Let's go" transitions to placeholder step 1; page.tsx returns null during hydration, then shows onboarding</verify>
  <done>Onboarding flow wired with page gate and step transitions</done>
</task>

</tasks>

<verification>
- npm run build passes with no errors
- First visit: diet screen shows with 3 cards
- Selecting a diet highlights the card (primary bg, shadow, white text)
- "Let's go" is disabled until a card is selected
- Clicking "Let's go" calls setDiet and transitions to step 1 (placeholder)
- Return visit (onboardingComplete = false in localStorage): same flow
- Return visit (onboardingComplete = true in localStorage): shows main app content, no onboarding flash
</verification>

<success_criteria>
- ONBR-01: User can select diet preference on first launch
- ONBR-04: Diet screen shows 3 vertical cards with icons
- Onboarding gate works: onboarding only shows when onboardingComplete is false
- Hydration gate works: no flash of onboarding for returning users
</success_criteria>

<output>
After completion, create .planning/phases/03-onboarding/03-01-SUMMARY.md
</output>
