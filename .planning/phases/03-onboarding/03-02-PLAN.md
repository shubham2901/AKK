---
phase: 03-onboarding
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - components/onboarding/OnboardingFlow.tsx
  - components/onboarding/BlocklistStep.tsx
autonomous: true
requirements: [ONBR-02, ONBR-03, ONBR-05]

must_haves:
  truths:
    - "User sees cuisine blocklist with asymmetric tag cloud and rotated chips"
    - "Multi-select: tapping a chip toggles it (selected/unselected)"
    - "I'm done button saves blocklist and completes onboarding"
    - "Skip option available — user can choose to exclude nothing"
    - "Onboarding data saved to localStorage; flow never shown again"
  artifacts:
    - path: components/onboarding/BlocklistStep.tsx
      provides: Cuisine blocklist selection screen
      exports: ["default"]
  key_links:
    - from: components/onboarding/BlocklistStep.tsx
      to: stores/session-store.ts
      via: useSessionStore setBlocklist, completeOnboarding
      pattern: "setBlocklist|completeOnboarding"
    - from: components/onboarding/OnboardingFlow.tsx
      to: components/onboarding/BlocklistStep.tsx
      via: step === 1
      pattern: "BlocklistStep"
---

<objective>
Build the cuisine blocklist screen with asymmetric rotated chip cloud and wire it into OnboardingFlow as step 2. Completing this step saves preferences and marks onboarding as done.

Purpose: Second and final onboarding screen — user picks cuisines to permanently exclude. After this, onboarding never shows again.
Output: components/onboarding/BlocklistStep.tsx, updated OnboardingFlow.tsx
</objective>

<execution_context>
@/Users/shubham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shubham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DESIGN_SYSTEM.md
@.planning/phases/03-onboarding/03-CONTEXT.md
@.planning/phases/03-onboarding/03-RESEARCH.md
@stores/session-store.ts
@components/onboarding/OnboardingFlow.tsx
@components/onboarding/DietStep.tsx
@stitch_screens/1_cuisine_exclusions.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BlocklistStep component</name>
  <files>components/onboarding/BlocklistStep.tsx</files>
  <action>
Create components/onboarding/BlocklistStep.tsx as a 'use client' component.

Props: `onComplete: () => void`

Internal state: `selected: Set<string>` (local state for toggling chips)

Constants — curated cuisine list:
```typescript
const CUISINES = [
  'North Indian', 'South Indian', 'Punjabi', 'Gujarati', 'Rajasthani',
  'Bengali', 'Maharashtrian', 'Kerala', 'Hyderabadi', 'Mughlai',
  'Chinese', 'Indo-Chinese', 'Italian', 'Street Food', 'Fusion',
] as const
```

Rotation pattern for chips — cycle through these based on index:
```typescript
const ROTATIONS = ['rotate-[2deg]', '-rotate-[1.5deg]', 'rotate-[3deg]', '-rotate-[2.5deg]', 'rotate-[1deg]']
```

Layout (reference stitch_screens/1_cuisine_exclusions.html, match vibe not pixels):
- Full height container: `min-h-screen flex flex-col max-w-md mx-auto p-6`
- Header row: back arrow button + "Step 2 of 2" text + spacer
  - Back arrow: `size-10 rounded-full border-2 border-charcoal flex items-center justify-center`
    - On tap: call onBack prop (or handle in OnboardingFlow by going to step 0)
    - Icon: material-symbols-outlined "arrow_back"
  - Step text: `text-sm font-bold tracking-widest uppercase`
- Headline section:
  - Title: "ANYTHING YOU'LL NEVER TOUCH?" using font-heading
    - "NEVER" in primary color, italic
    - Style: `text-4xl sm:text-5xl font-extrabold leading-[0.9] tracking-tight uppercase`
  - Subtitle: "We'll hide these forever. No judgment."
    - Style: `text-lg font-medium opacity-80 leading-snug mt-4`
- Asymmetric tag cloud: `flex flex-wrap gap-3 items-start content-start flex-1 mt-8 mb-24`
  - Each chip: `inline-block px-5 py-2.5 border-2 border-charcoal rounded-lg font-bold text-base min-h-[44px] flex items-center`
  - Apply rotation class from ROTATIONS[index % ROTATIONS.length]
  - Unselected: `bg-white text-charcoal`
  - Selected: `bg-primary text-white shadow-small` (shadow-small = 4px 4px 0px #181311)
  - On tap: toggle the cuisine in the selected Set
  - Use motion.button with whileTap={{ scale: 0.95 }} for tap feedback
- Bottom area: fixed to bottom with gradient fade background
  - "I'm done" button: full-width, bg-primary, border-2 border-charcoal, shadow-large, rounded-xl
    - Text: "I'm done" in heading font, white, bold, uppercase
    - Arrow icon: material-symbols-outlined "arrow_forward"
    - active:translate-y-1 active:shadow-none transition-all
    - On click: call `useSessionStore.getState().setBlocklist([...selected])` then `useSessionStore.getState().completeOnboarding()` then call `onComplete()`
  - Skip option: text link below or beside the button
    - Text: "Skip — I eat everything" or just "Skip"
    - Style: `text-sm font-semibold text-charcoal/60 mt-3 text-center`
    - On click: call `useSessionStore.getState().setBlocklist([])` then `useSessionStore.getState().completeOnboarding()` then call `onComplete()`
    - NOTE: Skip saves an empty blocklist, not null — this is intentional so we know the user made a choice

IMPORTANT: Use `useSessionStore.getState()` for the save actions (not hooks) since these are fire-once callbacks, not reactive state. This avoids unnecessary re-renders.
  </action>
  <verify>Component renders all 15 cuisines as chips with rotations; tapping toggles selected state; "I'm done" saves blocklist and calls onComplete; "Skip" saves empty blocklist and calls onComplete</verify>
  <done>Cuisine blocklist screen renders with asymmetric tag cloud and save functionality</done>
</task>

<task type="auto">
  <name>Task 2: Wire BlocklistStep into OnboardingFlow</name>
  <files>components/onboarding/OnboardingFlow.tsx</files>
  <action>
Update OnboardingFlow.tsx:

- Import BlocklistStep from './BlocklistStep'
- Replace the step 1 placeholder with BlocklistStep
- Props for BlocklistStep:
  - onComplete: no-op function (onboarding gate in page.tsx will automatically hide onboarding once completeOnboarding() is called and Zustand re-renders)
  - onBack: () => setStep(0)
- Update DietStep's onNext to also support going back:
  - Add onBack prop to OnboardingFlow's step rendering if needed (step 0 has no back)

The flow is:
1. step 0 (DietStep): user picks diet → setDiet() → setStep(1)
2. step 1 (BlocklistStep): user picks blocklist → setBlocklist() + completeOnboarding() → page.tsx re-renders, onboarding disappears

No explicit navigation after completeOnboarding — the page.tsx gate handles it reactively.
  </action>
  <verify>Full flow works: diet → select → "Let's go" → slide transition → blocklist → select chips → "I'm done" → onboarding disappears, main app content shows; also test: back arrow on blocklist returns to diet step; skip on blocklist completes onboarding with empty blocklist</verify>
  <done>Complete onboarding flow: diet → blocklist → done</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end verification</name>
  <files>none (verification only)</files>
  <action>
Verify the complete onboarding flow:

1. Clear localStorage (Application tab → Storage → Local Storage → delete akk-session)
2. Reload page — diet screen should appear
3. No card selected — "Let's go" should be disabled/dimmed
4. Tap "Vegetarian" card — card highlights with primary bg and shadow
5. Tap "Non-Veg" card — previous deselects, this one selects (single-select)
6. Click "Let's go" — slide transition to blocklist screen
7. Blocklist shows 15 cuisine chips with slight rotations
8. Tap a few chips — they toggle primary bg
9. Click "I'm done" — onboarding disappears, main app content shows
10. Reload page — onboarding does NOT appear (persisted)
11. Check localStorage akk-session: preferences.diet should be set, preferences.blocklist should be array, preferences.onboardingComplete should be true

Also test:
- Back arrow on blocklist → returns to diet screen
- "Skip" on blocklist → completes with empty blocklist
- npm run build passes with no errors
  </action>
  <verify>All 11 checks pass; npm run build clean</verify>
  <done>Onboarding flow complete and verified end-to-end</done>
</task>

</tasks>

<verification>
- npm run build passes with no errors
- Full onboarding flow: diet → blocklist → complete
- Preferences persisted to localStorage
- Onboarding never shown again after completion
- Back navigation works (blocklist → diet)
- Skip option works (empty blocklist saved)
- Chip rotations visible (asymmetric tag cloud aesthetic)
- All touch targets ≥ 44px
</verification>

<success_criteria>
- ONBR-02: User can select cuisine blocklist on first launch
- ONBR-03: Onboarding data saved to localStorage and never shown again
- ONBR-05: Blocklist screen shows asymmetric tag cloud with rotated chips
</success_criteria>

<output>
After completion, create .planning/phases/03-onboarding/03-02-SUMMARY.md
</output>
