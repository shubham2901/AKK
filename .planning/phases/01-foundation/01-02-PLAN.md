---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/globals.css
  - app/page.tsx
autonomous: true
requirements:
  - FOUN-01
  - FOUN-02
  - FOUN-03
  - FOUN-04

must_haves:
  truths:
    - "Design tokens (primary, charcoal, shadows, radius) visible in UI"
    - "App name 'Aaj Kya Khana Hai?' displayed with Syne heading style"
    - "Layout is mobile-first with max-w-md container"
    - "Interactive elements have min 44px touch targets"
  artifacts:
    - path: app/globals.css
      provides: Design tokens via @theme
      contains: "@theme"
    - path: app/page.tsx
      provides: Branded placeholder with tokens demo
      min_lines: 40
  key_links:
    - from: app/page.tsx
      to: app/globals.css
      via: Tailwind utilities (bg-primary, shadow-medium, etc.)
      pattern: "bg-primary|shadow-|text-charcoal|rounded-"
    - from: app/page.tsx
      to: layout
      via: font-heading and font-sans from @theme
      pattern: "font-heading|font-sans"
---

<objective>
Add design tokens to Tailwind and render the branded placeholder page demonstrating the neo-brutalist design system.

**Purpose:** Make FOUN-03 and FOUN-04 verifiable. The placeholder proves tokens work and establishes mobile-first layout patterns for downstream phases.

**Output:** globals.css with @theme (colors, shadows, radius, fonts); page.tsx with app name, tagline, token demo (chips, button with shadow), max-w-md container, touch-friendly targets.
</objective>

<execution_context>
@/Users/shubham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shubham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add design tokens to globals.css</name>
  <files>app/globals.css</files>
  <action>
Add @theme block to app/globals.css (after @import "tailwindcss") with tokens per CONTEXT and RESEARCH:

1. **Colors** (locked):
   - --color-primary: #e8612c
   - --color-charcoal: #181311
   - --color-bg-light: #FFF9F5
   - --color-bg-dark: #211511

2. **Shadows** (neo-brutalist offset, no blur):
   - --shadow-small: 4px 4px 0px #181311
   - --shadow-medium: 6px 6px 0px #181311
   - --shadow-large: 8px 8px 0px #181311

3. **Radius** (per CONTEXT):
   - --radius-default: 0.5rem
   - --radius-lg: 1rem
   - --radius-xl: 1.5rem
   - --radius-full: 9999px

4. **Fonts** (reference next/font CSS vars from layout):
   - --font-heading: var(--font-syne), ui-sans-serif, sans-serif
   - --font-sans: var(--font-body), ui-sans-serif, sans-serif

Preserve any existing base styles (e.g. body, html). Tailwind v4 generates utilities from @theme (bg-primary, shadow-medium, etc.).
  </action>
  <verify>npm run build succeeds; inspect page — bg-primary, shadow-medium, text-charcoal classes apply correctly</verify>
  <done>@theme block defines colors, shadows, radius, fonts; utilities work</done>
</task>

<task type="auto">
  <name>Task 2: Build branded placeholder page</name>
  <files>app/page.tsx</files>
  <action>
Replace app/page.tsx with branded placeholder per CONTEXT:

1. **Structure:**
   - main: min-h-screen flex flex-col items-center justify-center p-6 max-w-md mx-auto
   - Optional: border-x-2 border-charcoal to frame content (per CONTEXT specifics)

2. **App name (hero heading):**
   - "Aaj Kya Khana Hai?" in Syne, uppercase, font-extrabold (800), tracking-[-0.04em], leading-[0.85]
   - Use font-heading (or font-[family-name:var(--font-syne)] if needed)
   - class: text-charcoal, text-4xl or larger

3. **Tagline:**
   - Subheading in Plus Jakarta Sans (body font), normal case
   - e.g. "What's for dinner tonight?"

4. **Token demo:**

   a) **Chips (2–3 sample chips):**
   - border-2 border-charcoal, rounded (radius-default or radius-lg), shadow-small
   - min-h-[44px] min-w-[44px] or min touch target 44px — use padding to achieve
   - Background: bg-bg-light or white with border

   b) **Button:**
   - Primary CTA: bg-primary, border-2 border-charcoal, shadow-medium
   - active:translate-y-1 active:shadow-none (press effect per CONTEXT)
   - min-h-[44px] min-w-[44px] or equivalent touch target

5. **Touch targets (FOUN-04):**
   - All interactive elements: min-h-[44px] or min-w-[44px] where needed
   - Use p-3 (12px) minimum padding for 44px total height

6. **No icons:** Phase 1 ships without Material Symbols (deferred to Phase 3 per RESEARCH).

**Deferred:** Dark mode. Light only.
  </action>
  <verify>View page: app name visible in Syne; chips and button show tokens; resize to mobile viewport — layout is max-w-md centered; touch targets are at least 44px</verify>
  <done>Placeholder shows app name, tagline, token demo; mobile-first; touch-friendly</done>
</task>

</tasks>

<verification>
- Run `npm run build` — must succeed
- Open app in mobile viewport (375px width): no horizontal scroll; content centered; max-w-md container
- Inspect: chips use shadow-small, button uses shadow-medium; primary and charcoal colors visible
- Measure: buttons/chips have min 44px height (DevTools or inspect element)
</verification>

<success_criteria>
- FOUN-03: Design tokens applied globally and visible in placeholder
- FOUN-04: Mobile-first layout, max-w-md container, touch targets min 44px
- Placeholder demonstrates neo-brutalist style (hard borders, offset shadows)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
