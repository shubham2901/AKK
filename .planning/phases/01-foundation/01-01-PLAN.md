---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/globals.css
  - package.json
autonomous: true
requirements:
  - FOUN-01
  - FOUN-02

must_haves:
  truths:
    - "App loads with warm peachy background (#FFF9F5) immediately — no white flash"
    - "Syne and Plus Jakarta Sans load without FOUC or layout shift"
    - "theme-color meta tag matches app background for mobile status bar"
  artifacts:
    - path: app/layout.tsx
      provides: Root layout with fonts, body background, theme-color
      min_lines: 25
    - path: app/globals.css
      provides: Tailwind import and base styles
      contains: "@import"
    - path: package.json
      provides: Next.js 15, React 19, Tailwind dependencies
      contains: "next"
  key_links:
    - from: app/layout.tsx
      to: next/font
      via: Syne and Plus_Jakarta_Sans imports
      pattern: "from 'next/font/google'"
    - from: app/layout.tsx
      to: body
      via: inline backgroundColor style
      pattern: "backgroundColor.*FFF9F5"
---

<objective>
Bootstrap the Next.js app and configure the root layout to prevent white flash and font FOUC.

**Purpose:** Establish the app shell with instant background color and self-hosted fonts. Every subsequent phase builds on this foundation.

**Output:** Working Next.js 15 app with Tailwind v4, root layout with Syne + Plus Jakarta Sans via next/font, inline body background #FFF9F5, theme-color metadata.
</objective>

<execution_context>
@/Users/shubham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shubham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Next.js app</name>
  <files>package.json, app/layout.tsx, app/page.tsx, app/globals.css, next.config.ts</files>
  <action>
Bootstrap the app in the project root (current directory):

1. Run: `npx create-next-app@latest . --typescript --tailwind --eslint --app --yes`
   - Use `.` to create in current directory. If prompted about existing files, proceed (app/, public/ will be created).
   - Flags: TypeScript, Tailwind, ESLint, App Router. Omit --src-dir to use app/ in root (default).

2. If Tailwind v3 was scaffolded, upgrade to v4:
   - Run: `npx @tailwindcss/upgrade`
   - Follow prompts to migrate globals.css to @import "tailwindcss" and @theme if needed.

3. Verify: `npm run build` succeeds. App should load at localhost:3000 with default Next.js page.

**Avoid:** Do not use external Google Fonts link. Do not add next-themes. Do not use tailwind.config.js for theme (Tailwind v4 uses @theme in CSS).
  </action>
  <verify>npm run build succeeds; curl -s http://localhost:3000 2>/dev/null | head -20 shows HTML with body</verify>
  <done>Next.js 15 app exists with Tailwind v4; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Configure root layout — fonts, anti-flash, theme-color</name>
  <files>app/layout.tsx</files>
  <action>
Modify app/layout.tsx to implement FOUN-01 and FOUN-02:

1. **Fonts (next/font):**
   - Import Syne with `weight: ['400', '800']`, `subsets: ['latin']`, `variable: '--font-syne'`, `display: 'swap'`
   - Import Plus_Jakarta_Sans with `subsets: ['latin']`, `variable: '--font-body'`, `display: 'swap'`
   - Apply both via `className` on html: `className={\`${syne.variable} ${plusJakarta.variable}\`}`
   - Apply Plus Jakarta as body font: `className={plusJakarta.className}` on body

2. **Anti-flash (FOUN-01):**
   - Add `style={{ backgroundColor: '#FFF9F5' }}` on body — inline, not via Tailwind class. This ensures background is in HTML stream before CSS loads.

3. **theme-color:**
   - Add to metadata: `themeColor: '#FFF9F5'` (Next.js generates the meta tag). Use hex for iOS compatibility.

4. **Error Boundary (per RESEARCH):**
   - Wrap {children} in a simple error boundary component or use React error boundary. Minimal: catch and display fallback. Optional if time-constrained — layout must at least render.

**Locked per CONTEXT:** Syne weight 800 for headings; Plus Jakarta for body. Both via next/font only.
  </action>
  <verify>View page source: body has style="background-color: #FFF9F5" or equivalent; meta theme-color present; no link to fonts.googleapis.com</verify>
  <done>Layout has inline body bg, next/font applied, themeColor in metadata</done>
</task>

</tasks>

<verification>
- Run `npm run build` — must succeed
- Open app in mobile Chrome/Safari (or DevTools device emulation): no white flash on load; background is #FFF9F5 immediately
- Check Network tab: fonts loaded from _next/static (self-hosted), not Google CDN
</verification>

<success_criteria>
- FOUN-01: No white flash or layout jank on mobile load
- FOUN-02: Syne and Plus Jakarta Sans load without FOUC
- theme-color meta tag present and matches #FFF9F5
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
