---
phase: 06-discovery-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/discovery/DiscoveryCard.tsx
  - components/discovery/DiscoveryCardStack.tsx
autonomous: true
requirements: [DISC-01, DISC-02, DISC-05, DISC-06]

must_haves:
  truths:
    - "Full-screen card shows food photo, recipe name (Syne), one-line hook, 2-3 cuisine/diet chips"
    - "Swipe right = next card, swipe left = previous card"
    - "Swipe uses spring physics (overshoot, settle)"
    - "~20% screen width threshold commits swipe"
    - "Card layout matches immersive design (full-bleed photo, neo-brutalist text block)"
    - "One card visible at a time; loop at edges"
  artifacts:
    - path: components/discovery/DiscoveryCard.tsx
      provides: Single card with photo, text block, chips
    - path: components/discovery/DiscoveryCardStack.tsx
      provides: Stack container, drag, AnimatePresence, loop navigation
  key_links:
    - from: components/discovery/DiscoveryCardStack.tsx
      to: stores/session-store.ts
      via: filteredPool, nextCard, prevCard, setCurrentIndex
      pattern: "useSessionStore|filterPool|nextCard|prevCard"
    - from: components/discovery/DiscoveryCard.tsx
      to: lib/types/database.types.ts
      via: Recipe type
      pattern: "Recipe"
---

<objective>
Build DiscoveryCard (full-bleed photo, neo-brutalist text block) and DiscoveryCardStack (Motion drag, spring physics, loop navigation). One card visible at a time. Per CONTEXT: Charcoal text in hard border box, not white-on-gradient.

Purpose: Core discovery interaction — swipeable cards with physics. Phase 6-02 delivers the card UI and swipe behavior.
Output: DiscoveryCard.tsx, DiscoveryCardStack.tsx
</objective>

<execution_context>
@/Users/shubham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shubham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-discovery-loop/06-CONTEXT.md
@.planning/phases/06-discovery-loop/06-RESEARCH.md
@stores/session-store.ts
@lib/types/database.types.ts
@app/globals.css
@stitch_screens/3_recipe_card_immersive.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: DiscoveryCard component</name>
  <files>components/discovery/DiscoveryCard.tsx</files>
  <action>
Create DiscoveryCard component for a single recipe card.

**Props:** `recipe: Recipe`, `onTap?: () => void` (for tap-to-open detail overlay)

**Layout (per CONTEXT):**
- Full-bleed photo — image covers entire card. Use recipe.thumbnail or recipe title for fallback. Use next/image if URL is valid; else placeholder div. Set `draggable={false}` on img to prevent ghost image (per 06-RESEARCH.md Pitfall 3).
- Neo-brutalist text block at bottom — hard 2px border (border-charcoal), Charcoal text (not white-on-gradient per CONTEXT). Box with bg-bg-light or bg-white, border-2 border-charcoal, rounded per design tokens (--radius-default). Slight offset shadow optional (--shadow-small).
- Content: recipe name (Syne heading, font-heading, recipe_name_english or title), one-line hook (one_line_hook), 2-3 chips from cuisine_tags and diet_tags combined.
- One card visible — no peek at next/previous.

**Touch:** Add `touch-action: none` (Tailwind `touch-none`) to card container so drag works on mobile (per 06-RESEARCH.md Pitfall 1).

**Reference:** stitch_screens/3_recipe_card_immersive.html for layout vibe. CONTEXT overrides: Charcoal text in hard border box, not white-on-gradient.
  </action>
  <verify>Card renders full-bleed photo, neo-brutalist text block with recipe name, hook, chips; tap fires onTap; draggable={false} on img; touch-none on container; npm run build passes</verify>
  <done>DiscoveryCard renders recipe with correct layout and touch handling</done>
</task>

<task type="auto">
  <name>Task 2: DiscoveryCardStack with swipe and spring physics</name>
  <files>components/discovery/DiscoveryCardStack.tsx</files>
  <action>
Create DiscoveryCardStack that renders the current card and handles swipe.

**Data flow:**
- Derive filteredPool: `useMemo(() => filterPool(pool, cuisineFilter, mealTypeFilter), [pool, cuisineFilter, mealTypeFilter])` from session store.
- currentRecipe = filteredPool[currentIndex] (clamp index if filteredPool.length changed)
- If filteredPool.length === 0, show empty state (placeholder or "No recipes match your filters")

**Layout:**
- Full viewport or full parent. Single visible card.
- Use AnimatePresence with mode="wait" or "popLayout" for exit animation.
- `key={recipe.id}` (NOT index — per 06-RESEARCH.md Pitfall 2)

**Swipe (per 06-RESEARCH.md):**
- Motion `drag="x"` (horizontal only per research recommendation)
- useMotionValue(0) for x
- onDragEnd: check offset.x and velocity.x. Threshold ~20% screen width (e.g. 0.2 * window.innerWidth) or velocity > 500.
- If committed: offset > 0 → nextCard; offset < 0 → prevCard. Call animate(x, exitX (e.g. 400 or -400), { type: 'spring', stiffness: 300, damping: 30 }).then(() => { nextCard(filteredPool.length) or prevCard(filteredPool.length); x.set(0) })
- If not committed: animate(x, 0, { type: 'spring', stiffness: 400, damping: 40 })
- dragElastic={0.2}, dragConstraints={{ left: 0, right: 0 }} (or allow slight overshoot)
- dragTransition: bounceStiffness: 600, bounceDamping: 10 (playful per CONTEXT)

**Loop:** Pass filteredPool.length to nextCard(filteredPool.length) and prevCard(filteredPool.length) so wrap uses filtered length.

**Props:** `onCardTap?: (recipe: Recipe) => void` — pass to DiscoveryCard for Phase 7 overlay.
  </action>
  <verify>Swipe right commits to next; swipe left to previous; spring physics on commit and snap-back; loop at edges; key={recipe.id}; npm run build passes</verify>
  <done>DiscoveryCardStack renders single card with swipe, spring physics, loop navigation</done>
</task>

</tasks>

<verification>
- Full-screen card with full-bleed photo and neo-brutalist text block
- Swipe right/left navigates with spring physics
- ~20% threshold commits swipe
- Loop at first/last card
- AnimatePresence with key={recipe.id}
- touch-action: none, draggable={false} on img
</verification>

<success_criteria>
- DISC-01: Full-screen card with photo, name, chips, hook
- DISC-02: Swipe right/up = next, left/down = previous; loop
- DISC-05: Spring physics on swipe
- DISC-06: Layout matches immersive design (full-bleed, neo-brutalist text block)
</success_criteria>

<output>
After completion, create .planning/phases/06-discovery-loop/06-02-SUMMARY.md
</output>
