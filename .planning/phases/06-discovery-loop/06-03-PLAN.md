---
phase: 06-discovery-loop
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - components/discovery/FilterBar.tsx
  - app/page.tsx
autonomous: true
requirements: [DISC-03, DISC-04, DISC-07]

must_haves:
  truths:
    - "Shuffle button (top right) re-randomizes pool and resets position to 0"
    - "Filter icon opens FilterBottomSheet"
    - "Active filters shown as filled chips at top when filters applied"
    - "Tap anywhere on card opens Recipe Detail overlay (Phase 7 placeholder)"
    - "Discovery replaces placeholder when pool >= 5"
  artifacts:
    - path: components/discovery/FilterBar.tsx
      provides: Floating shuffle + filter icons, active filter chips
    - path: app/page.tsx
      provides: DiscoveryCardStack + FilterBar integration, tap-to-overlay
  key_links:
    - from: app/page.tsx
      to: components/discovery/DiscoveryCardStack.tsx
      via: Renders when pool >= 5
      pattern: "DiscoveryCardStack"
    - from: components/discovery/FilterBar.tsx
      to: components/discovery/FilterBottomSheet.tsx
      via: Opens on filter icon tap
      pattern: "FilterBottomSheet"
    - from: components/discovery/FilterBar.tsx
      to: stores/session-store.ts
      via: shufflePool, cuisineFilter, mealTypeFilter
      pattern: "shufflePool|cuisineFilter|mealTypeFilter"
---

<objective>
Create FilterBar (floating shuffle + filter icons, active chips) and integrate DiscoveryCardStack + FilterBar into page.tsx. Wire tap on card to Recipe Detail overlay placeholder. Replace "Discovery cards coming in Phase 6" with full discovery UI.

Purpose: Complete the discovery loop chrome and page integration. Phase 6-03 delivers the floating controls and tap-to-detail.
Output: FilterBar.tsx, updated app/page.tsx
</objective>

<execution_context>
@/Users/shubham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shubham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-discovery-loop/06-CONTEXT.md
@.planning/phases/06-discovery-loop/06-RESEARCH.md
@stores/session-store.ts
@app/page.tsx
@components/discovery/DiscoveryCardStack.tsx
@components/discovery/FilterBottomSheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: FilterBar with shuffle, filter icon, and active chips</name>
  <files>components/discovery/FilterBar.tsx</files>
  <action>
Create FilterBar component with minimal chrome per CONTEXT.

**Layout:**
- Floating over card (absolute or fixed, z-10). No header bar.
- Shuffle button: top-right. Material Symbol "shuffle". Use existing Material Symbols font from Phase 3. Button: rounded, subtle bg (e.g. bg-charcoal/10 or bg-white/10), border-2 border-charcoal. On click: shufflePool() from store.
- Filter icon: top-right, left of shuffle (or in a row). Material Symbol "tune" or "filter_list". On click: open FilterBottomSheet (useState for open).
- Active filter chips: when cuisineFilter.length > 0 or mealTypeFilter.length > 0, show filled chips at top of screen (below or beside the icons). Chips show selected values (e.g. "North Indian", "Lunch"). Styling: filled (bg-primary/20 or similar), border-charcoal. Per CONTEXT: "colored chips appear at top of screen when filters are active".

**FilterBottomSheet:**
- Render FilterBottomSheet with open state. Pass pool from store, onClose sets open to false.
- FilterBar receives pool as prop or reads from store.

**Positioning:** Padding (e.g. p-4 pt-6), z-index above card. Exact values at Claude's discretion per CONTEXT.
  </action>
  <verify>Shuffle button calls shufflePool; filter icon opens FilterBottomSheet; active chips visible when filters applied; npm run build passes</verify>
  <done>FilterBar renders shuffle + filter icons; active chips show when filters active</done>
</task>

<task type="auto">
  <name>Task 2: Page integration and tap-to-detail</name>
  <files>app/page.tsx</files>
  <action>
Replace discovery placeholder with DiscoveryCardStack + FilterBar. Wire tap to Recipe Detail overlay.

**Discovery branch (when setupComplete, pool >= 5, !fetchError):**
- Remove "Aaj Kya Khana Hai?" and "Discovery cards coming in Phase 6" placeholder.
- Render: Full-viewport container (min-h-screen, relative). DiscoveryCardStack fills the area. FilterBar overlays (absolute top-0 right-0 or similar).
- DiscoveryCardStack receives onCardTap callback.

**Tap-to-detail (DISC-03):**
- Add state: selectedRecipe: Recipe | null
- onCardTap: setSelectedRecipe(recipe)
- When selectedRecipe is set, render Recipe Detail overlay placeholder. Options:
  a) Simple overlay: fixed inset-0 z-50, semi-transparent bg, centered card with recipe name + "Detail coming in Phase 7" and a close button that sets selectedRecipe to null.
  b) Minimal: a div that says "Recipe Detail (Phase 7)" with recipe name, close button.
- Overlay should not block discovery; user can close and continue swiping.

**Layout:** Discovery should be full-screen immersive. Remove the motion.main border/padding that constrains the current placeholder. Use a wrapper that allows full-bleed cards. Check that max-w-md doesn't constrain the card — cards should be full viewport width on mobile.

**Empty filtered pool:** If filteredPool.length === 0 (filters applied, no matches), DiscoveryCardStack should show "No recipes match your filters" or similar. Ensure this is handled in DiscoveryCardStack (from 06-02) or add fallback here.
  </action>
  <verify>Discovery shows full-screen cards + FilterBar; tap opens overlay placeholder; close returns to cards; shuffle works; npm run build passes</verify>
  <done>Page shows DiscoveryCardStack with FilterBar; tap opens Recipe Detail overlay placeholder</done>
</task>

</tasks>

<verification>
- Shuffle re-randomizes pool and resets to card 0
- Filter icon opens FilterBottomSheet
- Active filter chips visible when filters applied
- Tap on card opens overlay; close returns to discovery
- Full-screen discovery layout
</verification>

<success_criteria>
- DISC-03: Tap anywhere on card opens Recipe Detail overlay
- DISC-04: Shuffle button re-randomizes pool and resets position
- DISC-07: Filter bar with shuffle + filter icon, bottom sheet, active chips
</success_criteria>

<output>
After completion, create .planning/phases/06-discovery-loop/06-03-SUMMARY.md
</output>
